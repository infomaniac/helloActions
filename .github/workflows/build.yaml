name: Build
on: 
  push
  

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v3

      - run: go version

      # - name: Test
      #   run: go test ./...
      # - name: Build
      #   env:
      #     GOOS: linux
      #     GOARCH: amd64
      #     CGO_ENABLED: 0
      #   run: go build